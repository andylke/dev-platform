services:
  loki:
    image: grafana/loki:${LOKI_VERSION}
    container_name: loki
    hostname: loki
    restart: unless-stopped

    networks:
      - tools

    volumes:
      - ./observability/loki/config/loki.yaml:/etc/loki/loki.yaml:ro
      - ./observability/loki/runtime/volumes/data:/loki

    command:
      - -config.file=/etc/loki/loki.yaml
