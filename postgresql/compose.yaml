services:

  postgresql:

    image: postgres:${POSTGRESQL_VERSION}
    container_name: postgresql
    restart: unless-stopped

    environment:
      POSTGRES_USER: ${POSTGRESQL_USER}
      POSTGRES_PASSWORD: ${POSTGRESQL_PASSWORD}
      POSTGRES_DB: ${POSTGRESQL_DB}
      TZ: ${TZ}

    ports:
      - "${POSTGRESQL_PORT}:5432"

    networks:
      - backend
      - tools

    volumes:
      - ./postgresql/runtime/certificates:/certificates:ro
      - ./postgresql/runtime/config/postgresql.conf:/etc/postgresql/postgresql.conf:ro
      - ./postgresql/runtime/config/pg_hba.conf:/etc/postgresql/pg_hba.conf:ro
      - ./postgresql/runtime/volumes:/var/lib/postgresql

    command: >
      postgres
      -c config_file=/etc/postgresql/postgresql.conf
      -c hba_file=/etc/postgresql/pg_hba.conf
